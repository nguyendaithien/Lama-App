import React from 'react';
import { Icon } from '@ui-kitten/components';

// export const HomeIcon = (props: any) => <Icon {...props} name="gateway" pack="assets" />;

export const HomeIcon = (props: any) => <Icon {...props} name="home-outline" />;

export const ProfileIcon = (props: any) => <Icon {...props} name="person-outline" />;

export const TeamIcon = (props: any) => <Icon {...props} name="people-outline" />;

export const ProjectIcon = (props: any) => <Icon {...props} name="briefcase-outline" />;

export const MoreIcon = (props: any) => <Icon {...props} name="more-horizontal-outline" />;

export const MailIcon = (props: any) => <Icon {...props} name="email-outline" />;

export const LockIcon = (props: any) => <Icon {...props} name="lock-outline" />;

export const BellIcon = (props: any) => <Icon {...props} name="bell-outline" animation="shake" />;
