import { Text, StyleSheet, View } from 'react-native';
import React, { Component } from 'react';

export const CreateMemberScreen = () => {
  return (
    <View>
      <Text>CreateMemberScreen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
